# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
 
  config.vm.box = "rhel-7.3-vbox" 
  config.vm.provider "virtualbox" do |vb|
      vb.gui = true
  end
 # config.vm.provision "shell", inline: <<-SHELL
 #   sudo mkdir newfolder 
  #  sudo yum update -y
 #   sudo yum install ansible -y
 # SHELL
end

